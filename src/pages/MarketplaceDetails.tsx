/**
 * src/pages/MarketplaceDetails.tsx — Bambeh Marketplace
 *
 * REWRITE — June 2026
 *
 * The previous version was a stub placeholder (emoji + "Détails de l'article").
 * This is now the fully functional item detail page, equivalent to
 * MarketplaceItemDetails.tsx but used where routes point to /marketplace/details/:id.
 *
 * If your router uses /marketplace/:id → MarketplaceItemDetails and
 * /marketplace/details/:id → MarketplaceDetails, both are now identical in
 * behaviour. You may safely delete this file and redirect
 * /marketplace/details/:id to /marketplace/:id instead.
 */

export { default } from "./MarketplaceItemDetails";
